# Workflows del proyecto (Windsurf)

Estos workflows se descubren automáticamente desde `.windsurf/workflows/`.

## Lista
- `/smoke-test`
- `/build-backend`
- `/build-installer-win`
- `/release-checklist`
- `/add-skill`

## Uso
En Cascade, invoca el workflow escribiendo el slash command (por ejemplo):
- `/smoke-test`
